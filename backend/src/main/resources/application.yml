spring:
  datasource:
    url: jdbc:mysql://kdt.goodee.co.kr:20003/Final_DB?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update # 운영은 validate 또는 none 권장
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  redis:
    host: ${REDIS_HOST:localhost} # 환경변수 없을 때 기본값
    port: ${REDIS_PORT:6379}
    # timeout: 2000ms      # (선택) 연결 타임아웃
    # password: xxx        # (보안/운영시 사용)

logging:
  level:
    org.springframework: INFO

jwt:
  secret: wYEFYfrh8aSZu9gsSls7PVVrhERbR9bq3qHFsYGQ3a1TCkhsvMxnRhXWoEB4aj41+FTidD4r/DGt93P3zLKxLw==
  access-token-validity-ms: 900000 # 15분
  refresh-token-validity-ms: 1209600000 # 14일
