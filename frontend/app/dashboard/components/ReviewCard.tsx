import styles from "../styles/MainSection.module.css";

export default function ReviewCard() {
  return (
    <section className={styles.subCard}>
      <span className={styles.subCardTitle}>인기 정책 후기</span>
    </section>
  );
}
