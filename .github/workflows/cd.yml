name: 🚀 Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx (multi-platform 지원 시)
        uses: docker/setup-buildx-action@v2

      - name: 🔐 Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: 🐳 Build & Push Backend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infra/docker/Dockerfile.backend
          push: true
          tags: jibangyoung/backend-prod:latest

      - name: 🐳 Build & Push Frontend Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infra/docker/Dockerfile.frontend
          push: true
          tags: jibangyoung/frontend-prod:latest

      - name: 🚀 Deploy to EC2 via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ubuntu
          key: ${{ secrets.EC2_KEY }}
          script: |
            sudo docker pull jibangyoung/backend-prod:latest
            sudo docker pull jibangyoung/frontend-prod:latest

            sudo docker stop jibangyoung-backend || true
            sudo docker rm jibangyoung-backend || true
            sudo docker run -d --name jibangyoung-backend -p 8080:8080 jibangyoung/backend-prod:latest

            sudo docker stop jibangyoung-frontend || true
            sudo docker rm jibangyoung-frontend || true
            sudo docker run -d --name jibangyoung-frontend -p 3000:3000 jibangyoung/frontend-prod:latest
